print("What is the area of the retangle? ")

base = float(input("What is the base? "))
height = float(input("What is the height? "))

area = base * height

print(f"Your area is: {area} unit of measurement")
